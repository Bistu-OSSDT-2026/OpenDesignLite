//! `artifact_create` → 等价 `odl new`，转发到 od-core。
//!
//! Spec: docs/specs/mcp.md（artifact_create）

use crate::error::McpError;

pub fn run() -> Result<(), McpError> {
    todo!("M2: create artifact via od-core; see docs/specs/mcp.md")
}
