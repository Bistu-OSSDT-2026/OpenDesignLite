//! `artifact_handoff` → 等价 `odl handoff`，生成或读取 handoff.md。
//!
//! Spec: docs/specs/mcp.md（artifact_handoff）

use crate::error::McpError;

pub fn run() -> Result<(), McpError> {
    todo!("M2: generate/read handoff via od-core; see docs/specs/mcp.md")
}
