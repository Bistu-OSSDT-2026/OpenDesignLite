---
name: html-page
mode: html
description: Create or improve a single-file HTML artifact with inline CSS and minimal JavaScript.
---

Produce a fast, self-contained HTML file. Prefer simple semantic markup, stable layout, and readable CSS. Avoid build steps unless the user explicitly asks for them.

Expected output:

- `index.html`
- optional `assets/`
- `handoff.md` summarizing intent and next steps
