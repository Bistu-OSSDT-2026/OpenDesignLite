Added a minimal GitHub Actions pipeline for the Rust workspace. Pushes and PRs to main run cargo fmt, clippy, and tests. Version tags starting with v build od-cli release binaries for Linux, macOS Intel/ARM, and Windows, upload workflow artifacts, and attach them to a GitHub Release.
